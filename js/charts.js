const colors=['#5b5bd6','#16a47a','#f0a33a','#dd5365','#4ba5db','#a263d8','#72b84b','#e079ad'];
export function bars(canvas,entries){const ctx=canvas.getContext('2d'),dpr=devicePixelRatio||1,w=canvas.clientWidth,h=230;canvas.width=w*dpr;canvas.height=h*dpr;ctx.scale(dpr,dpr);ctx.clearRect(0,0,w,h);if(!entries.length){ctx.fillStyle='#888';ctx.fillText('Sin datos en el período',20,40);return}const max=Math.max(...entries.map(x=>x[1]));entries.slice(0,8).forEach(([label,val],i)=>{const y=15+i*26,bw=(w-145)*val/max;ctx.fillStyle=colors[i%colors.length];ctx.fillRect(120,y,bw,15);ctx.fillStyle=getComputedStyle(document.body).color;ctx.font='12px system-ui';ctx.fillText(label.slice(0,16),4,y+12);ctx.fillText(Math.round(val),125+bw,y+12);});}
export function donut(canvas,available,protectedMoney){const ctx=canvas.getContext('2d'),dpr=devicePixelRatio||1,w=canvas.clientWidth,h=230;canvas.width=w*dpr;canvas.height=h*dpr;ctx.scale(dpr,dpr);const total=Math.max(available+protectedMoney,1),cx=w/2,cy=100,r=70;let start=-Math.PI/2;[available,protectedMoney].forEach((v,i)=>{ctx.beginPath();ctx.strokeStyle=colors[i];ctx.lineWidth=28;ctx.arc(cx,cy,r,start,start+Math.PI*2*v/total);ctx.stroke();start+=Math.PI*2*v/total});ctx.fillStyle=getComputedStyle(document.body).color;ctx.textAlign='center';ctx.font='bold 18px system-ui';ctx.fillText('Fondos',cx,106);ctx.font='12px system-ui';ctx.fillText('Disponible / protegido',cx,205);}
